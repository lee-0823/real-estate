import React, { useEffect, useRef, useState } from "react";
import styled from "@emotion/styled";

function BusinessIntro() {
  const bgColor = {
    backgroundColor: "black",
  };

  return <div style={bgColor}></div>;
}

export default BusinessIntro;
